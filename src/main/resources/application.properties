spring.application.name=proyecto-final-nat20

spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.datasource.username=nat20_owner
spring.datasource.password=npg_i98rWTjHMkoe
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://ep-noisy-cell-a50k4v6d-pooler.us-east-2.aws.neon.tech/nat20?user=nat20_owner&password=npg_i98rWTjHMkoe&sslmode=require

# Configuración de Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.enabled=true

# Configuración de logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
app.jwt.secret=tuClaveSecretaMuyLargaYSeguraParaFirmarLosTokensJWT123456789
app.jwt.expiration=3600000

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Configuración de CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080

# Configuración de seguridad
app.security.remember-me.key=clave_remember_me_segura
app.security.remember-me.validity-seconds=86400

# Configuración de cookies
app.cookie.secure=true
app.cookie.http-only=true
app.cookie.same-site=Strict

# Configuración de seguridad para cookies
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=strict

# Configuración de JPA
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.open-in-view=false